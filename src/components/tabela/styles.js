import styled from 'styled-components'
import '../../styles/tabela.scss'

const TabelaWrapper = styled.div.attrs({className:"tabela-alelo"})`

  div {
    overflow-x: auto;
  }
`

 export default TabelaWrapper;